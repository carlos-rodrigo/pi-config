# Context Engineering — Decisions

- Keep `AGENTS.md` concise and route deeper guidance to `docs/`.
- Keep `scripts/verify.sh` as the stable verification interface for agents.
- Keep `.features/` operational and ignored; use `docs/features/` only for durable product/system context.
- Prefer lightweight, on-demand feature packets over mandatory ceremony for every change.
