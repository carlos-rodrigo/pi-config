# Context Engineering — Proof

## Regression gate

```bash
bash scripts/verify.sh
```

Expected: exits 0 and stays silent on success.

## Spot checks

- `AGENTS.md` lists current commands and search/verification guidance.
- `docs/README.md` and `docs/features/README.md` describe the same feature packet shape.
- Extension inventories in `README.md` and `extensions/README.md` match shipped extension directories.
