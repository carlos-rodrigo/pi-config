# Review Hub Web App

React + Vite + Tailwind v4 workspace for Review Hub UI refresh.

## Build output

`npm run build` generates static assets in:

- `web-app/dist/index.html`
- `web-app/dist/assets/*`

This directory is intended to be served by `extensions/review-hub/lib/server.ts`.
