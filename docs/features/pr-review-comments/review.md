# PR-Native Document Review Comments — Review

This packet was migrated from legacy planning filenames to the current strategy-first feature packet shape.

Current implementation anchors live under `extensions/document-reviewer/`, including GitHub PR helpers, PR worktree handling, PR diff mapping, server PR session mode, and review-page PR UI behavior.
